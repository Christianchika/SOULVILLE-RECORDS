import Header from "@/components/Header";import Footer from "@/components/Footer";import {events} from "@/data/events";export default function Events(){return <><Header/><main className="section page"><div className="section-label">TOUR / EVENTS</div><h1 className="page-title">Live <em>dates.</em></h1><div className="event-list">{events.map(e=><article className="event-card" key={e.id}><div><div className="section-label">{new Date(e.date).toLocaleDateString("en",{month:"short",day:"2-digit",year:"numeric"})}</div><h2>{e.artist}</h2></div><div><strong>{e.venue}</strong><p>{e.city}, {e.country}</p></div><a className="button" href={e.ticketUrl}>Tickets</a></article>)}</div></main><Footer/></>}