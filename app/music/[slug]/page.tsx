import type {Metadata} from "next";import {releases} from "@/data/releases";import Header from "@/components/Header";import Footer from "@/components/Footer";import ReleaseCard from "@/components/ReleaseCard";export function generateStaticParams(){return releases.map(r=>({slug:r.slug}));}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const release=releases.find(r=>r.slug===slug);return release?{title:release.title,description:release.description}:{title:"Release not found"};}export default async function ReleasePage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const release=releases.find(r=>r.slug===slug);if(!release)return <><Header/><main className="page section"><h1>Release not found</h1></main><Footer/></>;const related=releases.filter(r=>r.slug!==release.slug).slice(0,2);return <><Header/><main><section className="profile"><div className="cover large">{release.title[0]}</div><div><div className="section-label">{release.type} · {release.year}</div><h1>{release.title}</h1><h3>{release.artist}</h3><p>{release.description}</p><div className="actions"><a className="button" href={release.streaming.spotify}>Spotify</a><a className="text-link" href={release.streaming.youtube}>YouTube →</a><a className="text-link" href={release.streaming.apple}>Apple Music →</a></div></div></section><section className="section"><div className="content-narrow"><div className="section-label">CREDITS</div><h2>Made with<br/><em>intention.</em></h2><ul>{release.credits.map(c=><li key={c}>{c}</li>)}</ul></div></section>{related.length>0&&<section className="section"><div className="section-heading"><div className="section-label">MORE RELEASES</div><h2>From the<br/><em>catalogue.</em></h2></div><div className="release-grid">{related.map(r=><ReleaseCard key={r.slug} release={r}/>)}</div></section>}</main><Footer/></>}