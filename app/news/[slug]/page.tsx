import {news} from "@/data/news";import Header from "@/components/Header";import Footer from "@/components/Footer";import NewsCard from "@/components/NewsCard";export function generateStaticParams(){return news.map(n=>({slug:n.slug}));}
export default async function Story({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const item=news.find(n=>n.slug===slug);if(!item)return <><Header/><main className="section page"><h1>Story not found</h1></main><Footer/></>;const related=news.filter(n=>n.slug!==item.slug).slice(0,2);return <><Header/><main><article className="section article"><div className="content-narrow"><div className="section-label">{item.category} · {item.date}</div><div className="news-hero-placeholder">{item.title[0]}</div><h1 className="page-title">{item.title}</h1><p className="article-intro">{item.excerpt}</p>{item.body.map((paragraph,i)=><p key={i} className="article-body">{paragraph}</p>)}</div></article>{related.length>0&&<section className="section"><div className="section-heading"><div className="section-label">RELATED STORIES</div><h2>More from<br/><em>Soulville.</em></h2></div><div className="news-grid">{related.map(n=><NewsCard key={n.slug} item={n}/>)}</div></section>}</main><Footer/></>}