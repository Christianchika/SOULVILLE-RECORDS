import {news} from "@/data/news";import Header from "@/components/Header";import Footer from "@/components/Footer";export function generateStaticParams(){return news.map(n=>({slug:n.slug}));}export default async function Story({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const item=news.find(n=>n.slug===slug);if(!item)return <><Header/><main className="section page"><h1>Story not found</h1></main><Footer/></>;return <><Header/><main className="section page"><div className="content-narrow"><div className="section-label">{item.date}</div><h1 className="page-title">{item.title}</h1><p>{item.excerpt}</p><h2>Story content</h2><p>This reusable article template is ready for the full editorial story, images, embeds and related releases.</p></div></main><Footer/></>}