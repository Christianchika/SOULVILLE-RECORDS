import Header from "@/components/Header";import Footer from "@/components/Footer";import NewsCard from "@/components/NewsCard";import {news} from "@/data/news";export default function News(){return <><Header/><main><section className="section page"><div className="section-label">NEWS / JOURNAL</div><h1 className="page-title">From<br/><em>Soulville.</em></h1><p className="page-lead">Stories, announcements, artist milestones and notes from the label.</p></section><section className="section"><div className="news-grid">{news.map(n=><NewsCard key={n.slug} item={n}/>)}</div></section></main><Footer/></>}