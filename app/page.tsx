import Header from "@/components/Header";import Footer from "@/components/Footer";import ArtistCard from "@/components/ArtistCard";import ReleaseCard from "@/components/ReleaseCard";import NewsCard from "@/components/NewsCard";import EventCard from "@/components/EventCard";import Newsletter from "@/components/Newsletter";import {artists} from "@/data/artists";import {releases} from "@/data/releases";import {news} from "@/data/news";import {events} from "@/data/events";
export default function Home(){const featured=releases[0];return <><Header/><main><section className="hero"><p className="eyebrow">INDEPENDENT MUSIC • EST. 2026</p><h1>Sound<br/><em>with soul.</em></h1><p className="hero-copy">A home for distinctive artists, honest stories, and records made to last.</p><div className="actions"><a className="button" href="/music">Explore the music</a><a className="text-link" href="/artists">Meet the artists →</a></div></section><section className="feature"><div className="section-label">01 / FEATURED RELEASE</div><div><div className="cover">{featured.title[0]}</div><p className="release-type">{featured.type} · {featured.year}</p><h2>{featured.title}</h2><p>{featured.description}</p><a className="text-link" href={"/music/"+featured.slug}>View release →</a></div></section><section className="section"><div className="section-heading"><div className="section-label">02 / ROSTER</div><h2>Artists with<br/><em>something to say.</em></h2></div><div className="artist-grid">{artists.map(a=><ArtistCard key={a.slug} artist={a}/>)}</div></section><section className="section"><div className="section-heading"><div className="section-label">03 / LATEST MUSIC</div><h2>The<br/><em>catalogue.</em></h2></div><div className="release-grid">{releases.slice(0,3).map(r=><ReleaseCard key={r.slug} release={r}/>)}</div></section><section className="story"><div className="section-label">04 / THE STORY</div><div><h2>Built around the music, not the noise.</h2><p>Soulville Records is an independent label built to give artists room to develop their sound, tell their stories and reach audiences without losing what makes them different.</p><a className="text-link" href="/about">Our story →</a></div></section><section className="section"><div className="section-heading"><div className="section-label">05 / UPCOMING</div><h2>See you<br/><em>out there.</em></h2></div>{events.length?events.slice(0,3).map((e,i)=><EventCard key={i} event={e}/>):<p>No upcoming dates yet.</p>}<div className="actions"><a className="text-link" href="/events">View all events →</a></div></section><section className="section"><div className="section-heading"><div className="section-label">06 / JOURNAL</div><h2>Inside<br/><em>Soulville.</em></h2></div><div className="news-grid">{news.slice(0,3).map(n=><NewsCard key={n.slug} item={n}/>)}</div></section><Newsletter/></main><Footer/></>}