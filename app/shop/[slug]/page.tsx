import {products} from "@/data/products";import Header from "@/components/Header";import Footer from "@/components/Footer";export function generateStaticParams(){return products.map(p=>({slug:p.slug}));}export default async function ProductPage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const product=products.find(p=>p.slug===slug);if(!product)return <><Header/><main className="section page"><h1>Product not found</h1></main><Footer/></>;return <><Header/><main><section className="profile"><div className="product-placeholder large">{product.category}</div><div><div className="section-label">{product.category}</div><h1>{product.name}</h1><p>{product.description}</p><div className="product-meta"><strong>{product.price}</strong><span>{product.status}</span></div><a className="button" href="/contact">Enquire about this item</a></div></section></main><Footer/></>}