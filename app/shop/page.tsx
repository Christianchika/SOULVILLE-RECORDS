import Header from "@/components/Header";import Footer from "@/components/Footer";import ProductCard from "@/components/ProductCard";import {products} from "@/data/products";export default function Shop(){return <><Header/><main><section className="section page"><div className="section-label">SHOP / MERCH</div><h1 className="page-title">Wear the<br/><em>sound.</em></h1><p className="page-lead">Vinyl, apparel, bundles and limited Soulville editions. Products shown here are placeholders until the official store launches.</p></section><section className="section"><div className="product-grid">{products.map(p=><ProductCard key={p.slug} product={p}/>)}</div></section><section className="cta"><p className="eyebrow">VINYL • APPAREL • LIMITED EDITIONS</p><h2>Something to<br/><em>take home.</em></h2><a className="button" href="/contact">Shop enquiries</a></section></main><Footer/></>}