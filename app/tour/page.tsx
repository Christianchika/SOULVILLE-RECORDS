import Header from "@/components/Header";import Footer from "@/components/Footer";import EventCard from "@/components/EventCard";import {events} from "@/data/events";
export default function Tour(){return <><Header/><main><section className="section page"><div className="section-label">TOUR / EVENTS</div><h1 className="page-title">Live<br/><em>dates.</em></h1><p className="page-lead">Upcoming shows, label nights and artist appearances. Placeholder events can be replaced from one central data file.</p></section><section className="section"><div className="section-heading"><div className="section-label">UPCOMING</div><h2>Meet us<br/><em>out there.</em></h2></div>{events.length?<div className="event-list">{events.map(e=><EventCard key={e.id} event={e}/>)}</div>:<div className="empty-state"><h2>No dates<br/><em>yet.</em></h2><p>New Soulville events will appear here when they are announced.</p></div>}</section><section className="cta"><p className="eyebrow">BOOKING • LIVE • EVENTS</p><h2>Bring the<br/><em>sound to your city.</em></h2><a className="button" href="/contact">Booking enquiries</a></section></main><Footer/></>}