"use client";import {useState} from "react";import {site} from "@/data/site";
export default function MobileNav(){const[open,setOpen]=useState(false);const links=[["Artists","/artists"],["Music","/music"],["Tour","/tour"],["About","/about"],["Contact","/contact"]];return <><button className="menu-toggle" type="button" aria-label={open?"Close navigation":"Open navigation"} aria-expanded={open} onClick={()=>setOpen(!open)}>{open?"Close":"Menu"}</button>{open&&<div className="mobile-menu"><nav aria-label="Mobile navigation">{links.map(([label,href])=><a key={href} href={href} onClick={()=>setOpen(false)}>{label}</a>)}</nav><div className="mobile-socials"><a href={site.socials.instagram}>Instagram</a><a href={site.socials.facebook}>Facebook</a><a href={site.socials.youtube}>YouTube</a><a href={site.socials.tiktok}>TikTok</a><a href={site.socials.whatsapp}>WhatsApp</a></div></div>}</>}