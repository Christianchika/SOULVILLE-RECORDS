export default function ProductCard({product}:{product:{slug:string;name:string;category:string;price:string;description:string;status:string}}){return <article className="product-card"><a href={"/shop/"+product.slug}><div className="product-placeholder">{product.category}</div></a><div className="section-label">{product.category}</div><h2>{product.name}</h2><p>{product.description}</p><div className="product-meta"><strong>{product.price}</strong><span>{product.status}</span></div><a className="button" href={"/shop/"+product.slug}>View product</a></article>}