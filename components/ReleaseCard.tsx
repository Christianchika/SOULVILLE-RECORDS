export default function ReleaseCard({release}:{release:{slug:string;title:string;artist:string;type:string;year:string}}){return <article className="release-card"><a href={"/music/"+release.slug}><div className="cover">{release.title[0]}</div></a><p>{release.type} · {release.year}</p><h2>{release.title}</h2><span>{release.artist}</span><br/><a className="text-link" href={"/music/"+release.slug}>View release →</a></article>}